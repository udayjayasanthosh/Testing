import React from "react";
import { View,Text } from "react-native";
import { Button } from "react-native-paper";
export default function Audddio()
{
     return(
            <>
            <View>
                <Button mode="contained" onPress={()=>{}} >
                    Unlock
                </Button>
            </View>
            </>
        )
}